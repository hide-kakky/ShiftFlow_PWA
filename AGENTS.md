# Codex ローカル運用メモ

- このリポジトリでは `CODEx_PROMPT.md` を常に最優先で参照すること。
- フロントのファイルを一行でも変更したら `frontend/public/sw.js` の `APP_VERSION` を必ず上げ、回答に新しい値を明記する。
- すべての回答で日本語のコミットメッセージ案と `git commit` コマンド例を提示する。
- 回答は日本語で行い、コピペで動く差分と実行手順・検証手順・ロールバック手順をセットで示す。
- コード修正がなくても、依頼があれば `APP_VERSION` を確認し回答に記載する。
